//This program is of character io, macros are there getchar() and putchar() can be 
//specially for charaters io.
#include <stdio.h>
void main()
{
    char ch;
    printf("Enter a  character:");
    ch = getchar();
    printf("You have entered: ");
    putchar(ch);
}